services:
  froment-software:
    container_name: froment-software
    build:
      context: 'https://github.com/sachahjkl/froment.software.git#main'
      dockerfile: Dockerfile
    ports:
      - '8080:80'
    restart: unless-stopped
