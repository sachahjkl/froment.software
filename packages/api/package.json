{
  "name": "@froment/api",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts src/migrate.ts --bundle --platform=node --format=cjs --external:better-sqlite3 --external:playwright-core --out-extension:.js=.cjs --outdir=dist",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "node dist/migrate.cjs",
    "start": "node dist/main.cjs",
    "test": "pnpm --filter @froment/documents build && pnpm --filter @froment/web build && pnpm build && vitest run --coverage",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@froment/contracts": "workspace:*",
    "@froment/documents": "workspace:*",
    "better-sqlite3": "catalog:",
    "drizzle-orm": "catalog:",
    "effect": "catalog:",
    "playwright-core": "catalog:",
    "ulid": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "@types/node": "catalog:",
    "@vitest/coverage-v8": "catalog:",
    "drizzle-kit": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
