{
  "name": "@froment/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --format=cjs --external:better-sqlite3 --outfile=dist/main.cjs",
    "db:generate": "drizzle-kit generate",
    "start": "node dist/main.cjs",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@froment/contracts": "workspace:*",
    "better-sqlite3": "catalog:",
    "drizzle-orm": "catalog:",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/better-sqlite3": "catalog:",
    "@types/node": "catalog:",
    "drizzle-kit": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
