{
  "name": "@froment/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc --noEmit && esbuild src/main.ts --bundle --platform=node --format=cjs --outfile=dist/main.cjs",
    "start": "node dist/main.cjs",
    "test": "pnpm build && vitest run"
  },
  "dependencies": {
    "@effect/platform-node": "catalog:",
    "@froment/contracts": "workspace:*",
    "effect": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "esbuild": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
